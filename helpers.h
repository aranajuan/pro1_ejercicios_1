#ifndef HELPERS_H_INCLUDED
#define HELPERS_H_INCLUDED

#include<string.h>

#define FLUSH() int c; while((c = getchar()) != '\n' && c != EOF);

void eliminar_caracter(char *, char);


#endif // HELPERS_H_INCLUDED
